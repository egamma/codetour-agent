{
    "title": "VSCode Cat Chat Agent Extension Tour",
    "steps": [
        {
            "file": "extension.ts",
            "line": 1,
            "description": "We start by importing the necessary modules from the `vscode` package."
        },
        {
            "file": "extension.ts",
            "line": 3,
            "description": "We define a constant for the command ID that will be used later in the extension."
        },
        {
            "file": "extension.ts",
            "line": 5,
            "description": "We define an interface for the result of our chat agent. It extends the `vscode.ChatAgentResult2` interface and adds a `slashCommand` property."
        },
        {
            "file": "extension.ts",
            "line": 9,
            "description": "The `activate` function is called when your extension is activated. It is here that we set up our chat agent."
        },
        {
            "file": "extension.ts",
            "line": 12,
            "description": "We define a handler for our chat agent. This function will be called whenever a user interacts with our agent."
        },
        {
            "file": "extension.ts",
            "line": 65,
            "description": "We create our chat agent using the `vscode.chat.createChatAgent` function. We pass in the name of our agent ('cat') and our handler function."
        },
        {
            "file": "extension.ts",
            "line": 66,
            "description": "We set the icon for our chat agent using the `iconPath` property. We use the `vscode.Uri.joinPath` function to create a URI to the icon file."
        },
        {
            "file": "extension.ts",
            "line": 67,
            "description": "We set the description for our chat agent using the `description` property. This description will be shown to users in the chat interface."
        },
        {
            "file": "extension.ts",
            "line": 78,
            "description": "Finally, we add our chat agent and the command handler to the extension's subscriptions. This ensures that they are cleaned up when the extension is deactivated."
        }
    ]
}